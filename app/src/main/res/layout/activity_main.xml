<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Scrollable wakelock output -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:background="#FF000000">

        <TextView
            android:id="@+id/wakelockTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#00FF00"
            android:fontFamily="monospace" />
    </ScrollView>

    <!-- Floating battery stats (top-left) -->
    <TextView
        android:id="@+id/batteryStatsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:background="#AA000000"
        android:fontFamily="monospace"
        android:layout_marginStart="10dp"
        android:layout_marginTop="350dp" />

    <!-- Scrollable button panel (bottom-left corner) -->
    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="10dp"
        android:layout_gravity="bottom|start"
        android:background="#66000000">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/clearLogButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Clear Log"
                android:textColor="#FFFFFF"
                android:backgroundTint="#FF4444"
                android:elevation="4dp" />

            <!-- Future optimization buttons can be added here -->

        </LinearLayout>
    </ScrollView>

</FrameLayout>
